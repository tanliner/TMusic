<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

    <ImageView
            android:id="@+id/iv_collector_item_preview"
            android:layout_width="@dimen/collector_item_preview_img_w"
            android:layout_height="@dimen/collector_item_preview_img_h"
            android:layout_centerVertical="true"
            android:layout_alignParentStart="true"
            android:contentDescription="@string/app_name" />

    <TextView
            android:id="@+id/tv_collector_item_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:lines="1"
            tools:text="@string/app_name"
            android:layout_centerVertical="true"
            android:layout_toEndOf="@id/iv_collector_item_preview" />

    <TextView
            android:id="@+id/tv_collector_item_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:lines="1"
            tools:text="@string/collector_item_count"
            android:textColor="@color/color_collector_item_count"
            android:layout_centerVertical="true"
            android:layout_toEndOf="@id/tv_collector_item_name"
            android:layout_marginStart="@dimen/collector_item_count_margin_start" />

</merge>